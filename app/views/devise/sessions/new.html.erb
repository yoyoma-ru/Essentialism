<div class="row">
  <div class="col-md-offset-3 col-md-6">
    <div class="row">
      <h2>ユーザーログイン</h2>
    </div>
    <div class="row">
      <p class="col-md-offset-1 col-md-10">
        メールアドレスと<br>パスワードを入力して下さい
      </p>
    </div>
    <div class="row">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="form-group col-md-offset-3 col-md-6">
          <label class="controll-label sr-only" for="inputEmail">Email</label>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", id:"inputEmail", class:"col-md-8 col-md-offset-2 form-controll", placeholder: "メールアドレス" %>
        </div>

        <div class="form-group col-md-offset-3 col-md-6">
          <label class="controll-label sr-only" for="inputPassword">Password</label>
          <%= f.password_field :password, autocomplete: "current-password", id:"inputPassword", class:'col-md-8 col-md-offset-2', placeholder: "パスワード" %>
        </div>

        <% if devise_mapping.rememberable? %>
          <div class="field col-md-offset-4 col-md-6">
            <%= f.check_box :remember_me %>
            <%= f.label :remember_me %>
          </div>
        <% end %>

        <div class="actions">
          <%= f.submit "サインイン", class:'btn btn-primary col-md-offset-4 col-md-4 btn' %>
        </div>
      <% end %>
    </div>
  </div>
</div>