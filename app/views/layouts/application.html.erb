<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Essentialism</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://kit.fontawesome.com/fff3ba2388.js" crossorigin="anonymous"></script>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body role="document" data-spy="scroll" data-target="#aboutScrollSpy" data-offset="300">

    <div class="container-fluid space_bottom">
      <div class="row header">
        <div class="col-xs-2 logo-image"></div>
        <div class="col-xs-offset-1 col-xs-6 title">エッセンシャル思考</div>
        <% if user_signed_in? %>
            <div class="col-xs-2 guest-name">
              "<%= link_to " #{current_user.name} ", user_path(current_user.id) %>"様
            </div>
        <% else %>
            <div class="col-xs-2 guest-name">ようこそ<br>” ゲスト ”様</div>
        <% end %>
        <!-- ハンバーガーメニュー -->
        <div id="hm-icon">
          <a class="menu-trigger" href="#">
            <span></span>
            <span></span>
            <span></span>
            <div class="menu-text">MENU</div>
          </a>
        </div>
        <div id="sp-menu">
          <h1>MENU</h1>
          <% if user_signed_in? %>
            <ul>
              <li><%= link_to "My Page", user_path(current_user.id) %></li>
              <li><%= link_to "Learns", learns_path %></li>
              <li><%= link_to "My Works", works_path %></li>
              <li><%= link_to "User Works", user_works_path %></li>
              <li>Favorites</li>
              <li><%= link_to "Log Out", destroy_user_session_path, method: :delete %>
            </ul>
          <% else %>
            <ul>
              <li><%= link_to "Sign in", new_user_registration_path %></li>
              <li><%= link_to "Log in", new_user_session_path %></li>
              <li><%= link_to "Learn", learns_path %></li>
              <li><%= link_to "About", about_path %></li>
            </ul>
          <% end %>
        </div>
        <!-- ハンバーガメニュー -->
      </div>

      <!-- ↓fixedの余白を埋めるbox↓ -->
      <div class="row space_top"></div>

      <%= yield %>
    </div>

    <div class="container-fluid">
      <div class="row footer"> ©2020/Essentialism/All Rigth Reserved</div>
    </div>

  </body>
</html>
