<div class="row">
	<div class="col-xs-offset-1 col-xs-10">
		<div class="row space">
	        <div class="col-xs-1 step-logo">step3</div>
	        <div class="col-xs-offset-1 col-xs-8">
		        <h1 class="work-title">本質目標を考える</h1>
		        <div class="row">
		        	<div class="col-xs-6">
		        		<p>Step2で答えた「人生の軸」に沿った<br>
		        			具体的で測量可能かつ刺激的で魅力的な<br>
		        			本質目標を考えるワークです。
		        		</p>
		        		<br>
		        		<p>本質目標を理解するためには<br>
		        			それ以外を理解することが近道になるため<br>
		        			右横に2×2のマトリックスを用意しているので参考にして下さい。
		        		</p>
		        	</div>
		        	<div class="col-xs-6">table作成</div>
		        </div>
		    </div>
		</div>

		<div class="row space">
			<div class="col-xs-3">
				<h4>人生の軸</h4>
		    </div>
		    <div class="col-xs-7 essential">mostの人生の軸を反映</div>
		</div>

		<div class="row space">
			<div class="col-xs-3">
				<h4>本質目標</h4>
		    </div>
		    <% if Work.where(user_id: current_user.id).exists?(genre: 7) == true %>
		        <div class="col-xs-7">
		    	    <%= form_with model: @my_essential_goal do |f| %>
		    	        <%= f.text_area :writing, class:"col-xs-12 essential", rows:"3", placeholder: "ここに本質目標を書きましょう" %>
		    	        <%= f.hidden_field :user_id, :value => current_user.id %>
		    	        <%= f.hidden_field :genre, :value => 7 %>
		    	        <%= f.submit "更新する", class:"pull-right" %>
		    	    <% end %>
		        </div>
		    <% else %>
		        <div class="col-xs-7">
		    	    <%= form_with model: @essential_goal do |f| %>
		    	        <%= f.text_area :writing, class:"col-xs-12 essential", rows:"3", placeholder: "ここに本質目標を書きましょう" %>
		    	        <%= f.hidden_field :user_id, :value => current_user.id %>
		    	        <%= f.hidden_field :genre, :value => 7 %>
		    	        <%= f.submit "作成", class:"pull-right" %>
		    	    <% end %>
		        </div>
		    <% end %>
		</div>

		<div class="row space">
			<%= link_to "戻る", 'javascript:history.back()', class:"col-xs-offset-1 col-xs-2 btn btn-default" %>
			<%= link_to "My Work一覧", works_path, class:"col-xs-offset-2 col-xs-2 btn btn-default" %>
			<%= link_to "次へ", works_6_path, class:"col-xs-offset-2 col-xs-2 btn btn-default" %>
	</div>
</div>